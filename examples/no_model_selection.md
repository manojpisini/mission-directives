# No model selected example

`md.py select-model` returns `no_selection` because no production-eligible measured profile exists. The operator may run benchmarks or manually select a model and record the exception. The runtime does not invent reliability values or silently choose the synthetic test profile.
