# Unresolved skill fallback example

A dashboard route prefers `dashboard-builder`, but its lock entry is unresolved. Automatic selection is blocked. The route records the rejected skill and continues through the native dashboard prompt, followed by independent visual and data verification.
