# Audit–Fix–Verify Loop

The loop target may be any investigative/executive pair or standalone audit workflow. A new pass is allowed only when prior remediation changes reachable evidence or leaves a finite residual queue. Each cycle freezes findings, applies one bounded authorized batch, reruns independent verification, and invokes `MD-198`. Repeating an unchanged audit against unchanged state is prohibited.
