# Example: Strudel Composition and Refinement

## Request

Use `/strudle` to create a reproducible 45-second live-coded intro theme with three sections. Refine only while timing, arrangement, or code-validity criteria improve.

## Alias resolution

```text
strudle → strudel
```

## Route

```text
MD-85 production brief
→ MD-192 skill fit
→ MD-196 (`strudel`)
→ optional MD-197 inner refinement loop
→ MD-198 exit gate
```

The result must be Strudel code, not a claim that audio was rendered when no renderer was executed. A refinement pass needs a concrete timing, arrangement, parsing, or reproducibility hypothesis.
